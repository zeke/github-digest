# Cloudflare AI Gateway (Unified Billing) — see AGENTS.md for how to create these.
CLOUDFLARE_API_KEY=""
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_GATEWAY_ID=""

# GitHub PAT with `notifications` + `repo` (read) scope.
DIGEST_GITHUB_TOKEN=""

# Cloudflare Email Sending token, scoped to `Email Sending: Edit`.
CF_EMAIL_TOKEN=""
